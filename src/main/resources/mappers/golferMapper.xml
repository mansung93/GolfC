<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.ord//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace ="golfc.mappers.golferMapper">
	<!--  golferList()
	select * from golfer order by id -->
	<select id="selectList" resultType="vo.GolferVO">
	select * from golfer
	</select>
</mapper>